---
- hosts: all
  become: yes
  gather_facts: yes
  tasks:
  - name: I want to install git, tree, wget by using same tasks
    apt:
      name: "{{item}}"
      state: absent
    loop: 
      - git
      - tree
      - wget
